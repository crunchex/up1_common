# This config file defines the current topology of the UP1 robot.
# It can be manually edited until there is a better mechanism for module discovery.

# Currently supported modules:
#   base:
#     - base
#   manipulator:
#     - mono_arm
#   io:
#     - io

up1:
  modules:
    base: base
    manipulator: mono_arm
    io: io
